{% set name = "prawn-svg" %}
{% set version = "0.40.3" %}

package:
  name: rb-{{ name|lower }}
  version: {{ version }}

source:
  - url: https://rubygems.org/downloads/{{ name }}-{{ version }}.gem
    sha256: 26237d3a7268143b5ffc6c1354ab93d0a4b81366573c749b6316e6f6f7733c0c

build:
  noarch: generic
  number: 0
  script:
    - gem install -N -l -V --norc --ignore-dependencies {{ name }}-{{ version }}.gem
    - gem unpack {{ name }}-{{ version }}.gem

requirements:
  host:
    - ruby
  run:
    - {{ pin_compatible('ruby', max_pin='x.x') }}
    - rb-css_parser ~=1.6
    - rb-matrix ~=0.4.2
    - rb-prawn >=0.11.1,<3
    - rb-rexml >=3.4.2,<4

test:
  commands:
    - ruby -r {{ name }} -e 'exit 0'


about:
  home: https://rubygems.org/gems/prawn-svg
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Prawn renders SVG directly into PDF documents.
  doc_url: http://www.rubydoc.info/gems/prawn-svg
  dev_url: https://github.com/mogest/prawn-svg

extra:
  recipe-maintainers:
    - jdmarques
    - BIT-cypher
